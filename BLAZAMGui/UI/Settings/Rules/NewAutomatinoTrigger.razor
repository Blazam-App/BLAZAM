@using BLAZAM.Database.Models.Rules


@code{
    [Parameter]
    public AutomationRule Rule { get; set; }

    private AutomationRuleAndFilter newAndFilter = new();
    private AutomationRuleOrFilter newOrFilterList = new();

    protected override void OnInitialized()
    {
        base.OnInitialized();
    }
}