@using System.DirectoryServices;
@using System.Security.Authentication
@using System.DirectoryServices.Protocols
@inherits AppComponentBase
<MudCard Class="ma-5 mx-auto pa-3">
    <MudCardHeader>
        <MudText Class="w-100"
        Align="Align.Center"
        Typo="@Typo.h6">
            @AppLocalization[Lang.Active_Directory_Connection_Status]
        </MudText>
    </MudCardHeader>

    <MudGrid Spacing="0" Class="mx-auto mud-container-maxwidth-xs">
        <MudItem xs="8">
            @AppLocalization[Lang.Server_Port_Open]
        </MudItem>
        <MudItem xs="4">
            <StatusCheck Status="_portStatus" />
        </MudItem>

        <MudItem xs="8">
            @AppLocalization[Lang.Credentials_OK]

        </MudItem>
        <MudItem xs="4">
            <StatusCheck Status="_credStatus" />
        </MudItem>

        <MudItem xs="8">
            @AppLocalization[Lang.Connected]
        </MudItem>
        <MudItem xs="4">
            <StatusCheck Status="Directory.Status==DirectoryConnectionStatus.OK" />
        </MudItem>


        <MudItem xs="8">
            @AppLocalization[Lang.Connection_Type]
        </MudItem>
        <MudItem xs="4">
            <StatusCheck Status=@GetConnectionTypeStatus()
            Text="@GetConnectionType()" />
        </MudItem>

        <MudItem xs="8">
            @AppLocalization[Lang.Encrypted]
        </MudItem>
        <MudItem xs="4">
            <StatusCheck Status=@GetSslStatus() />
        </MudItem>


        @if (OperatingSystem.IsWindows())
        {
            <MudItem xs="8">
                @AppLocalization[Lang.Connection_Type]
            </MudItem>
            <MudItem xs="4">
                <StatusCheck Status=@GetEncryptionTypeStatus()
                             Text="@GetEncryptionType()" />
            </MudItem>
        }
    </MudGrid>
    @if (Directory.Status != DirectoryConnectionStatus.OK && Directory.Status != DirectoryConnectionStatus.Connecting)
    {
        <MudChip Color="Color.Warning" Size="Size.Small" T="string" Text="@AppLocalization[Directory.Status.ToString()]" />
    }
    @if (Directory.ConnectionException != null)
    {
        var exception = Directory.ConnectionException;

        <MudAlert Severity="Severity.Error">

            <MudText Inline="false">@AppLocalization[Lang.Error]:</MudText>
            <br />
            <MudText Inline="false" Class="mt-3" Typo="Typo.caption">@exception.Message</MudText>

            @if (exception is DirectoryServicesCOMException comException)
            {
                <br />

                <br />
                <MudText Inline="false" Class="mt-3" Typo="Typo.caption">@comException.ExtendedErrorMessage</MudText>
            }
        </MudAlert>
    }
    <MudButton OnClick="@(RefreshStatuses)"
    Color="Color.Primary">
        @AppLocalization[Lang.Refresh]
    </MudButton>
    @if (showProgress)
    {
        <MudStack Row=true>
            <MudProgressLinear Indeterminate=true />

        </MudStack>
    }

</MudCard>

@code {

        [Parameter]
        public EventCallback DirectoryConnected {get; set; }

        bool _monitoring = false;
        bool? _portStatus = null;
        bool? _credStatus = null;
        bool showProgress = true;
    private AppLdapConnection? _connection;
    protected override async Task OnInitializedAsync()
    {
        await base.OnInitializedAsync();
        ResetStatus();
        Directory.OnStatusChanged += (async (state) => { await UpdateStatus(state); });
        await UpdateStatus(null);

    }
    public override void Dispose()
    {
        base.Dispose();
        _monitoring = false;
        _connection?.Dispose();
    }
    public async Task RefreshStatuses()
    {
        await Directory.CancelCheckConnection();
        ResetStatus();
        using var connection = await Directory.GetConnectionAsync();
    }
    public void ResetStatus()
    {
        Directory.CheckConnectionAsync();
        _portStatus = false;
        _credStatus = null;
        showProgress = true;
        StateHasChanged();
    }


    public async Task UpdateStatus(object? state)
    {
        await Task.Run(async () =>
        {
            if (_monitoring) return;
            _monitoring = true;

            while (_monitoring)
            {
                UpdateProgress();

                try
                {
                   await UpdatePortAndCredentials();
                }
                catch (UnresolvableAddressException)
                {
                    //ignore DNS errors, will show from Directory.ConnectionException
                }

                await StateHasChangedAsync();
                await Task.Delay(1000);
            }
        });
    }

    private void UpdateProgress()
    {
        if (Directory.Status == DirectoryConnectionStatus.Connecting)
        {
            showProgress = true;
            _ = StateHasChangedAsync();
        }
    }

    private async Task UpdatePortAndCredentials()
    {
        if (Directory.PortOpen)
        {
            _portStatus = true;
            await StateHasChangedAsync();

            if (Directory.Status == DirectoryConnectionStatus.OK)
            {
                _credStatus = true;
                showProgress = false;
                await InvokeAsync(DirectoryConnected.InvokeAsync);
            }
            else
            {
                _credStatus = false;
            }
        }
        else
        {
            _portStatus = false;
        }

        showProgress = Directory.Status == DirectoryConnectionStatus.Connecting;
    }

    private bool? GetConnectionTypeStatus()
    {
        if (Directory.Status != DirectoryConnectionStatus.OK) return false;
        if (Directory.AppRootDirectoryEntry == null) return false;
        var rootDE =  Directory.AppRootDirectoryEntry;
        var authType = rootDE.AuthenticationType;
        if (authType.Equals(AuthType.Negotiate) ||
            (authType.Equals(AuthType.Basic) &&
            rootDE.SslEnabled)) return true;
        return false;
    }
    private bool? GetEncryptionTypeStatus()
    {
        if (Directory.Status != DirectoryConnectionStatus.OK) return false;
        if (Directory.AppRootDirectoryEntry == null) return false;
        var authType = Directory.AppRootDirectoryEntry.EncryptionType;
        if (authType.Equals(CipherAlgorithmType.Aes256) ||
            authType.Equals(CipherAlgorithmType.Aes192)) return true;
        return false;
    }
    private bool? GetSslStatus()
    {
        if (Directory.Status != DirectoryConnectionStatus.OK) return false;
        if (Directory.AppRootDirectoryEntry == null) return false;
        return Directory.AppRootDirectoryEntry.SslEnabled;
    }
    private string? GetEncryptionType()
    {
        if (Directory.Status != DirectoryConnectionStatus.OK) return null;
        return Directory.AppRootDirectoryEntry?.EncryptionType.ToString();
    }
    private string? GetConnectionType()
    {

        if (Directory.Status != DirectoryConnectionStatus.OK) return null;
        return Directory.AppRootDirectoryEntry?.AuthenticationType.ToString();
    }
}
