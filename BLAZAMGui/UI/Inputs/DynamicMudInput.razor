@inherits MudField
@typeparam T

@switch (_type?.Name)
{
    case "int":
    case "Int32":
    case "double":
    case "long":
    case "float":
        <MudNumericField HelperText="@HelperText"
                         T=T
                         Label="@Label"
                         Immediate=@Immediate
                         HideSpinButtons="false"
                         @bind-Value="@Value"
                         ReadOnly=@ReadOnly
                         Clearable=@Clearable />
        break;

    case "String":
        <MudTextField Label="@Label" T=T
                      HelperText="@HelperText"
                      Immediate=@Immediate
                      Clearable=@Clearable
                      ReadOnly=@ReadOnly
                      Value=Value
                      ValueChanged="@((changed)=>{Value =changed;})"
                      Disabled=@Disabled
                      InputType="@InputType" />
        break;
    case "Boolean":
        <MudStack>
            <MudSwitch Color=@Color
                       UncheckedColor=@UncheckedColor
                       T="T"
                       ReadOnly=@ReadOnly
                       Label=@Label
                       @bind-Value=@Value
                       Disabled=@Disabled />
            <MudText Typo="Typo.caption">@HelperText</MudText>
        </MudStack>
        break;
    case "DateTime":
        <MudDatePicker Clearable=@Clearable
                       HelperText="@HelperText"
                       ReadOnly=@ReadOnly
                       Label="@Label"
                       Date="@dateValue"
                       DateChanged=@((date) =>{dateValue = date;})
                       Disabled=@Disabled />

        break;
    default:

        throw new AppException("The input type: " + _type?.Name + " could not be matched with a MudInput component");

}


@code {
    private T? objValue;


    [Parameter]
    public T? Value
    {
        get => objValue;
        set
        {
            if (objValue?.Equals(value) == true
            || (EqualityComparer<T?>.Default.Equals(objValue, default(T?))
            && EqualityComparer<T?>.Default.Equals(value, default(T?))))
            {
                return;
            }
            objValue = value;
            ValueChanged.InvokeAsync(value);

        }
    }

    [Parameter]
    public EventCallback<T?> ValueChanged { get; set; }

    [Parameter]
    public Color Color { get; set; } = Color.Success;
    [Parameter]
    public Color UncheckedColor { get; set; } = Color.Error;


    /// <summary>
    /// Show clear button.
    /// </summary>
    [Parameter]
    public bool Clearable { get; set; }
    /// <summary>
    /// Will be a read only field if applicable
    /// </summary>
    [Parameter]
    public bool ReadOnly { get; set; }

    [Parameter]
    public bool Immediate { get; set; } = true;

    private DateTime? dateValue
    {
        get
        {
            if (Value is DateTime time && time != DateTime.MinValue)
                return time;
            return null;
        }
        set
        {

            if (value is T tValue)
                Value = tValue;
            else if (value is null)
                Value = default;
        }
    }

    [Parameter]
    public InputType InputType { get; set; }

    private Type? _type;

    protected override void OnInitialized()
    {
        base.OnInitialized();
        _type = typeof(T);
        if (_type.IsGenericType && _type.GetGenericTypeDefinition() == typeof(Nullable<>))
        {
            _type = Nullable.GetUnderlyingType(_type);

        }
    }

}
