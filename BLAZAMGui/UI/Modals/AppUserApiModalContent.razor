
@inherits AppModalContent
<MudLink Href="/swagger" Target="_blank">
    <MudAlert Severity="Severity.Info"
    Variant="Variant.Filled"
    Class="mb-5"

    Icon="@Icons.Material.Filled.BookOnline">
        @AppLocalization[Lang.Click_to_view_API_documentation].
    </MudAlert>
</MudLink>
<UserApiTokenDataGrid @ref=apiDataGrid/>
@code{

    UserApiTokenDataGrid? apiDataGrid;
    protected override void OnInitialized()
    {
        base.OnInitialized();
        Modal.SetYesText(AppLocalization[Lang.Save]);
        Modal.OnYes += SaveSettings;
        Modal.YesEnabled = () => { return !LoadingData; };
    }
    private Task SaveSettings()
    {
        apiDataGrid?.Save();
        return Task.CompletedTask;
    }
}