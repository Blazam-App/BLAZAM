<MudText>Ldap Count:@SecureLdapConnector.Count</MudText>
@code{
    protected override void OnInitialized()
    {
        base.OnInitialized();
        SecureLdapConnector.OnCountChanged.Delegate+= (a,b) => InvokeAsync(StateHasChanged);
    }
}