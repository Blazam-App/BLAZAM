@inherits Widget
@attribute [Authorize]
<div Overflow="Overflow.Auto" Style="max-height:350px;">
    @foreach (var user in DisabledUsers)
    {

        <MudCard @onclick="@(()=>{Nav.NavigateTo(user.SearchUri);})">


            @user.DisplayName <br>
            @user.SAMAccountName <br>
            @user.OU.ToPrettyOu() <br>

            @user.LastChanged <br>
        </MudCard>

    }
</div>
