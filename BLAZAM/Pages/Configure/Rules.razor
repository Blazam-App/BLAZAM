@page "/rules"
@inherits AppComponentBase
@attribute [Authorize(Roles = UserRoles.SuperAdmin)]
<AppPageTitle>@AppLocalization["Rules"]</AppPageTitle>
<SetHeader>

    </SetHeader>
<MudText Typo="Typo.h4">@AppLocalization["Rules"] <AppDocumentationButton Page="/admin/rules" /></MudText>

<MudText>Rules is a concept for automation within Blazam. It would allow automated actions based on filtered event triggers.</MudText>
@code {

}
